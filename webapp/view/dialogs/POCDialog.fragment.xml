<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns="sap.m">
    <Dialog
        title="Mark POC Utilization"
        contentWidth="500px">
        <content>
            <VBox class="sapUiMediumMargin">
                <!-- POC Title -->
                <Label text="POC Title:" required="true" />
                <Input
                    value="{pocDialog>/pocTitle}"
                    placeholder="e.g., AI Chatbot POC, Blockchain Integration POC"
                    width="100%"/>
                
                <!-- Start Date -->
                <Label text="Start Date:" required="true" class="sapUiSmallMarginTop" />
                <DatePicker
                    value="{pocDialog>/startDate}"
                    placeholder="Select start date"
                    displayFormat="MM/dd/yyyy"
                    width="100%"/>
                
                <!-- End Date -->
                <Label text="End Date:" required="true" class="sapUiSmallMarginTop" />
                <DatePicker
                    value="{pocDialog>/endDate}"
                    placeholder="Select end date"
                    displayFormat="MM/dd/yyyy"
                    width="100%"/>
                
                <!-- Hours per Day Input -->
                <Label text="Hours per Day:" required="true" class="sapUiSmallMarginTop" />
                <StepInput
                    value="{pocDialog>/hoursPerDay}"
                    min="0"
                    max="24"
                    step="0.5"
                    displayValuePrecision="1"
                    width="100%"
                    description="hours"/>
            </VBox>
        </content>
        <beginButton>
            <Button
                text="Save"
                type="Emphasized"
                press="onSavePOC" />
        </beginButton>
        <endButton>
            <Button
                text="Cancel"
                press="onClosePOCDialog" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
