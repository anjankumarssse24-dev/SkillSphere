{
  "name": "skillsphere-ui",
  "version": "1.0.0",
  "description": "SkillSphere UI5 Application",
  "private": true,
  "devDependencies": {
    "@ui5/cli": "^4.0.33",
    "ui5-tooling-transpile": "^3.3.7",
    "typescript": "^5.9.3",
    "rimraf": "^5.0.5",
    "bestzip": "^2.2.1"
  },
  "scripts": {
    "build": "ui5 build --clean-dest --dest dist",
    "apply:variant": "node ../scripts/set-ui-variant.js",
    "build:ui": "npm run clean && npm run apply:variant && npm run build && npm run zip",
    "build:ui:test": "set UI_VARIANT=test&& npm run build:ui",
    "build:ui:prod": "set UI_VARIANT=prod&& npm run build:ui",
    "zip": "cd dist && npx bestzip skillsphere.zip *",
    "clean": "rimraf dist skillsphere.zip"
  },
  "ui5": {
    "dependencies": [
      "ui5-tooling-transpile"
    ]
  }
}