{
  "name": "skillsphere-ui",
  "version": "1.0.0",
  "description": "SkillSphere UI5 Application",
  "private": true,
  "scripts": {
    "build": "ui5 build --clean-dest --dest dist --exclude-task=generateComponentPreload",
    "build:ui": "ui5 build --clean-dest --dest dist && cd dist && npx bestzip ../skillsphere.zip *",
    "clean": "rimraf dist skillsphere.zip"
  }
}